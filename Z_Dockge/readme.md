# How to use Dockge

```sh
#https://jalcocert.github.io/Linux/docs/debian/docker/

git clone https://github.com/JAlcocerT/Docker ./Docker_Repo #apt install git
cd Docker_Repo/Z_Dockge

#curl https://raw.githubusercontent.com/louislam/dockge/master/compose.yaml --output compose.yaml #already downloaded
docker compose up -d #localhost:5001
```